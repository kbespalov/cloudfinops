// Browser stub for Node built-ins pulled in transitively by @diplodoc/transform
// (via @gravity-ui/aikit markdown rendering). These paths are never executed
// client-side; we only need them to resolve to an empty module in the browser.
module.exports = {};
